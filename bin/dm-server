#!/usr/bin/env node

const program = require('commander');

program
  .usage('[options] appName')
  .option('-t, --target <target>', 'Disable npm install after files created')
  .option('-o, --open', 'open browser')
  .parse(process.argv);


require('../server')(program);
